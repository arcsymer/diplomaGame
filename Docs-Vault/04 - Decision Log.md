# 04 — Decision Log (ADR)

> Журнал архитектурных и продуктовых решений: проблема → варианты → выбор → причина + источники. Накапливается, не переписывается. Формат: ADR-NNN.

---

## ADR-001 — Версия Unity: 6000.4.9f1 (изначально рекомендована 6.3 LTS)
**Дата:** 2026-06-10 · **Статус:** принято, дополнено в тот же день

**Проблема:** выбрать версию Unity для проекта со сроком сдачи (диплом).

**Варианты:**
1. Unity 6.0 LTS — поддержка до октября 2026 (кончится до защиты).
2. **Unity 6.3 LTS (6000.3.17f1)** — текущая LTS, поддержка до декабря 2027.
3. Unity 6.4 (Supported) — новые фичи (встроенный ECS, DirectStorage), но поддержка умрёт с выходом 6.5.

**Рекомендация ресёрча:** Unity 6.3 LTS — официальная рекомендация Unity для новых проектов; двухлетний хвост багфиксов покрывает весь срок диплома.

> **UPD 2026-06-10 — итоговое решение:** пользователь уже создал проект на **Unity 6000.4.9f1 (6.4, Supported)**. Остаёмся на 6.4: проект инициализирован, пакеты совместимы (URP 17.4, Cinemachine 3.1.6, Input System 1.19, AI Navigation 2.0.12), EditMode-тесты 3/3 зелёные. **Принятый риск:** поддержка 6.4 закончится с выходом 6.5 (~конец 2026); при блокирующих багах возможен переход на 6.3 LTS. Версию редактора не меняем до конца диплома.

Источники: [support schedule](https://unity.com/releases/unity-6/support) · [6.3 LTS blog](https://unity.com/blog/unity-6-3-lts-is-now-available)

---

## ADR-002 — Cinemachine 3.1, не 2.x
**Дата:** 2026-06-10 · **Статус:** принято (Фаза 0)

**Проблема:** Cinemachine 3.x кардинально сменил API относительно 2.x (большинство туториалов в сети — про 2.x).

**Выбор:** Cinemachine **3.1.x** — verified-версия для Unity 6.3. Работать только по документации 3.x: namespace `Unity.Cinemachine`, единый `CinemachineCamera` вместо `CinemachineVirtualCamera`/`FreeLook`, pipeline-компоненты как обычные MonoBehaviour, Channels вместо слоёв. **Причина:** 2.x — legacy; начинать новый проект на устаревшем API бессмысленно.

Источник: [Upgrade guide 2.x→3.x](https://docs.unity3d.com/Packages/com.unity.cinemachine@3.1/manual/CinemachineUpgradeFrom2.html)

---

## ADR-003 — UI: uGUI (Canvas + TMP) для HUD, UI Toolkit не используем в рантайме
**Дата:** 2026-06-10 · **Статус:** принято (Фаза 0)

**Проблема:** uGUI vs UI Toolkit для игрового UI (RTS HUD: миникарта, панель выделения, health bars над юнитами; TPS HUD: прицел, кулдауны; меню/настройки).

**Варианты:**
1. **uGUI всё** — зрелый, огромная база рецептов (миникарта = RawImage+RenderTexture, health bars = world-space canvas), твинится PrimeTween, дружит со сторонними ассетами.
2. UI Toolkit всё — современный стек, но world-space UI появился только в 6.2/6.3 (свежая фича = риск).
3. Гибрид (uGUI HUD + UI Toolkit меню) — две системы = двойная сложность стилей/анимаций.

**Выбор:** uGUI для всего. **Причина:** минимизация рисков в дедлайн-проекте; TextMeshPro теперь встроен в uGUI 2.0; официальная позиция Unity допускает uGUI как полностью поддерживаемый путь. Пересмотрим, только если упрёмся в производительность data-heavy экранов (не ожидается).

Источники: [UI comparison](https://docs.unity3d.com/Manual/UI-system-compare.html) · [World-space UI Toolkit](https://docs.unity3d.com/6000.3/Documentation/Manual/ui-systems/world-space-ui.html)

---

## ADR-004 — Твининг: PrimeTween вместо DOTween
**Дата:** 2026-06-10 · **Статус:** принято (Фаза 0)

**Варианты:** DOTween (популярнейший, но closed-source, аллокации, NRE при уничтожении целей) / **PrimeTween** (MIT, zero-allocation, безопасен при destroy) / LitMotion (MIT, быстрейший, data-oriented).

**Выбор:** PrimeTween. **Причина:** open-source MIT (чистая лицензионная история для диплома), zero-alloc (плюс к разделу о производительности), API проще LitMotion для UI-juice. DOTween отвергнут из-за closed-source и известных NRE-ловушек.

Источники: [PrimeTween](https://github.com/KyryloKuzyk/PrimeTween) · [Tween benchmark](https://github.com/AnnulusGames/TweenPerformance)

---

## ADR-005 — ИИ юнитов: собственная FSM/utility-AI, пакет Behavior не используем
**Дата:** 2026-06-10 · **Статус:** принято (Фаза 0)

**Проблема:** com.unity.behavior (behavior trees) с мая 2026 переведён в maintenance-only (команда сокращена ещё в феврале 2025).

**Выбор:** собственная лёгкая FSM / utility-AI для юнитов. **Причина:** зависимость от полузаброшенного пакета — риск; своя архитектура ИИ («умные юниты» — ядро темы сниженного микроменеджмента) — это содержательная часть диплома, её лучше иметь под полным контролем и описать в架 Architecture.md.

Источники: [Behavior maintenance announcement](https://discussions.unity.com/t/update-on-behavior-package-support-and-team-presence/1718517)

---

## ADR-006 — Аудио: только CC0/CC-BY библиотеки + локальная генерация; все облачные сервисы отвергнуты
**Дата:** 2026-06-10 (пересмотрено в тот же день под ADR-009) · **Статус:** принято (Фаза 0)

**Проблема:** выбрать пайплайн музыки, SFX и голосов юнитов с чистыми лицензиями при **нулевом бюджете** (ADR-009: без подписок, триалов, карт).

**Варианты и почему отпали:**
- **Udio** — скачивание треков отключено на всех тарифах после сделки с UMG → непригоден физически.
- **Suno** — нет официального API; free-тир без скачивания (сделка с Warner); платный Pro запрещён ADR-009.
- **ElevenLabs** (TTS/SFX/музыка) — freemium-подписка → запрещён ADR-009. *(Первая редакция этого ADR рекомендовала Starter $6/мес — отменено.)*
- **Stable Audio API / Azure TTS F0** — платный API / требует облачный аккаунт → запрещены ADR-009.

**Выбор (всё $0, без аккаунтов):**
- Музыка: incompetech/Kevin MacLeod (CC-BY 4.0, кредит в титрах) + Pixabay + OpenGameArt (CC0/CC-BY); опционально локальная генерация **Stable Audio Open** (открытые веса, Stability Community License — свободно при < $1M выручки).
- SFX: Kenney (CC0) + Sonniss GDC 2026 (royalty-free) ≈ 90%; точечно Freesound (бесплатный API, фильтр CC0/CC-BY); ретро/UI — sfxr/Bfxr/ChipTone локально.
- Голоса юнитов: путь №1 — **Kenney Voiceover Pack / (Fighter)** (CC0, готовые ack-реплики); путь №2 — **Piper TTS** локально (код GPL, лицензию конкретного голоса проверять на HF rhasspy/piper-voices).
- **MusicGen** — только запасной вариант: веса **CC-BY-NC** (для учебного диплома допустимо, для коммерческого распространения — нет; фиксирую согласно ADR-009/zero-budget §Лицензии).

**Причина:** соответствие нулевому бюджету; лицензионная чистота для публичного репозитория; Kenney Voiceover закрывает голоса вообще без генерации.

Источники: см. [[Research/00 - Prerequisites|Prerequisites, раздел B]]

---

## ADR-007 — Арт: CC0-набор Kenney + Quaternius; платные паки исключены
**Дата:** 2026-06-10 (пересмотрено в тот же день под ADR-009) · **Статус:** принято (Фаза 0)

**Выбор:** Kenney (Blaster Kit, Space Kit, UI Pack Sci-Fi, Particle Pack) + Quaternius (Animated Mech, Tanks, Ultimate Modular Sci-Fi) — всё CC0, можно коммитить в публичный репозиторий. Добор: Poly Pizza, Sketchfab (фильтр CC0); прототип-геометрия — примитивы + ProBuilder (бесплатный пакет Unity). Шрифты Russo One + Exo 2 (OFL, кириллица есть; Orbitron отвергнут — нет кириллицы). Иконки game-icons.net (CC-BY 3.0, атрибуция) + Kenney UI (CC0).

**Synty POLYGON ($49.99) — отвергнут** по ADR-009 (нулевой бюджет). Дополнительный аргумент: EULA Synty всё равно запрещает исходники в публичном репозитории. Бесплатные ассеты Unity Asset Store — тоже под Asset Store EULA (исходники в публичный репо нельзя), поэтому предпочтение строго CC0-источникам.

**Причина:** бюджет $0; юридически чистый открытый репозиторий; Kenney и Quaternius стилистически совместимы (low-poly).

Источники: см. [[Research/00 - Prerequisites|Prerequisites, раздел C]]

---

## ADR-008 — VCS: выборочный LFS с первого коммита; CI — минимальный и опциональный
**Дата:** 2026-06-10 · **Статус:** принято (Фаза 0), развилка по CI за пользователем

**Выбор:**
- LFS включаем до первого коммита (миграция задним числом = переписывание истории), но **выборочно**: крупные бинарники в LFS, мелкие (<~5 МБ png/ogg) — обычный git. Причина: free-тир 10 GiB хранилища + 10 GiB трафика/мес; data packs отменены; каждый clone/CI-checkout жжёт трафик.
- Билд распространяем через **GitHub Releases** (2 GiB/файл, трафик бесплатен), не через LFS.
- CI (GameCI unity-builder@v4): Actions для публичных репо бесплатны, но нужен секрет UNITY_LICENSE (действие пользователя) и сборки медленные. ~~Рекомендация: стартуем без CI~~ → **UPD 2026-06-10: пользователь решил делать CI/CD.** Внедрено: `tests.yml` (EditMode+PlayMode на push/PR) + `build-release.yml` (Windows x64 по тегу `v*` → автоматический GitHub Release). Основной путь сборки всё равно локальный (Project Forge); CI — верификация и релизы. До добавления секретов `UNITY_LICENSE`/`UNITY_EMAIL`/`UNITY_PASSWORD` workflow будет падать — ожидаемо. *(UPD: секреты добавлены 2026-06-10.)*

> **UPD 2026-06-10 (CI-образ):** Docker-образа `unityci/editor` для 6000.4.9f1 не существует (GameCI пропустил этот патч: есть 6000.4.8f1 и 6000.4.10f1). Решение: `customImage` с пином на **6000.4.10f1** в обоих workflow. Принятый риск: расхождение локального редактора (.9) и CI (.10) в пределах патч-версии — несущественно; при удобном случае локальный редактор можно обновить до 6000.4.10f1 для полного паритета.

Источники: см. [[Research/00 - Prerequisites|Prerequisites, раздел E]]

---

## ADR-009 — Жёсткое ограничение: нулевой бюджет проекта
**Дата:** 2026-06-10 · **Статус:** принято (директива пользователя, `zero-budget-constraint.md`) · **Приоритет: выше всех остальных решений**

**Суть:** бюджет проекта — $0. Запрещены: покупки, подписки, платные API, платные ассеты, а также **любые триалы/freemium с картой, сроком или конвертацией в платное**. Разрешены только: FOSS (бесплатно навсегда, без карты), локальная/self-hosted генерация, CC0/свободные ассеты, постоянно-бесплатные сервисы без карты (Unity Personal, GitHub, Git LFS free tier, gh CLI, Obsidian, Node.js).

**Последствия для ранее принятых решений:**
- ADR-006 пересмотрен: ElevenLabs/Stable Audio API/Azure исключены → CC0/CC-BY библиотеки + локальная генерация (Stable Audio Open, Piper).
- ADR-007 пересмотрен: Synty исключён → строго CC0-арт.
- Лицензионная политика: приоритет CC0 > CC-BY (с атрибуцией) > CC-NC (только если нет свободного аналога, помечать отдельно — для учебной работы допустимо, для коммерческого распространения нет). Всё внешнее — в `Licenses & Attribution.md`; ничего с неясной лицензией.

**Проверка перед выбором любого инструмента:** бесплатен навсегда? без карты? не триал? Если бесплатен только trial — отклонить и найти FOSS/CC0-аналог с фиксацией здесь.

---

## ADR-010 — Цели боевого ИИ: `Health` как общая сущность юнитов и зданий
**Дата:** 2026-06-11 (M10) · **Статус:** принято

**Проблема:** В исходной реализации `UnitCombat.ScanForTarget()` искал цели только через `UnitRegistry`. Здания в него не входили. Следствие: юниты не атаковали здания противника — волна доходила до базы и стояла. Победа волнами была принципиально недостижима (M10-BUG-001, выявлен интеграционными тестами `MatchSimulationTests`).

**Варианты:**
1. Добавить здания в `UnitRegistry` — смешение ответственности.
2. Дать `UnitCombat` отдельный список зданий — дублирование логики поиска ближайшего.
3. **Унифицировать через `Health`**: `UnitCombat` собирает кандидатов из `UnitRegistry` и `BuildingRegistry`, оборачивает их в общий буфер `List<Health>` + `List<Vector3>`, применяет единый `CombatLogic.FindNearestTargetIndex`.

**Выбор:** вариант 3. **Причина:** `Health` — уже общий компонент юнитов и зданий (оба реализуют `IDamageable`). Рефакторинг не меняет интерфейс — меняется только источник списка кандидатов. Дополнительно: для зданий добавлен буфер `BuildingAttackBuffer = 2f` к `attackRange` (здания крупные, без буфера юнит не «входит» в dистанцию атаки). Определение «цель — здание» — по отсутствию `NavMeshAgent` на цели (дёшево, без тега).

---

## ADR-011 — Авто-скриншоты: `ScreenshotDirector` + CLI-аргумент `-autoshot`
**Дата:** 2026-06-11 (M11) · **Статус:** принято

**Проблема:** скриншоты для README нужны из обоих режимов (RTS и TPS). Делать их вручную — ненадёжно (разные запуски, разные состояния). CI-сборка должна уметь произвести скриншоты без интерактивности.

**Варианты:**
1. Ручные скриншоты в редакторе (ненадёжно, не воспроизводимо).
2. Unity Test Framework с `ScreenCapture` (ограничен PlayMode, нет контроля над Cinemachine-блендом).
3. **`ScreenshotDirector` — компонент на сцене, активируется только при наличии CLI-аргумента `-autoshot <dir>`**.

**Выбор:** вариант 3. Реализация: компонент засыпает 2.5 с (прогрев сцены) → снимает RTS → `SetMode(Tps)` → ждёт 3.5 с (кроссфейд Cinemachine ~2 с + запас) → снимает TPS → `Application.Quit()`. `[RuntimeInitializeOnLoadMethod]` обеспечивает переход MainMenu → Sandbox при старте с `-autoshot`. **Причина:** воспроизводимо, не мешает нормальной работе, встроено в релизный билд без условной компиляции.

---

## ADR-012 — Нормализация масштабов FBX по баундам вместо ручной подгонки
**Дата:** 2026-06-11 (M11) · **Статус:** принято

**Проблема:** Kenney FBX-модели имеют разные единицы масштаба (часть в сантиметрах, часть в метрах, часть в других единицах). Ручная подгонка `localScale` каждого префаба — неидемпотентная работа, которую нельзя воспроизвести batch-вызовом.

**Варианты:**
1. Ручной подбор `Scale Factor` в Import Settings для каждой модели.
2. Нормализация через `ModelImporter.globalScale` в пакетном Editor-скрипте.
3. **Нормализация по баундам в Forge**: при Setup Visual читаем `Renderer.bounds.size` нового Visual-child и вычисляем `localScale` так, чтобы наибольшее из измерений bounding box соответствовало заданному целевому размеру (1 м для юнитов, 2 м для зданий).

**Выбор:** вариант 3. **Причина:** идемпотентен (Project Forge кнопка), не трогает Import Settings (не сломает GUID), автоматически корректирует любой будущий FBX. Единственный минус — bounding box может быть не идеальным (Т-поза персонажа шире вертикали), поэтому для юнитов берём наибольшее измерение и ограничиваем снизу по оси Y. На практике погрешность < 10% — приемлемо для диплома.

---

## ADR-013 — Реальные способности героя 2–4: Ударная волна, Ремонтное поле, Перегрузка
**Дата:** 2026-06-11 (Сессия 02, фаза улучшения) · **Статус:** принято

**Проблема:** DoD промпта v2 требует «2–4 способности» героя; фактически в v1.0.0 реален только Dash, слоты 2–4 — `Placeholder` с `Debug.Log`. Гэп-анализ (Improvements/01) пометил это как Р-1 — главный разрыв DoD.

**Варианты дизайна слотов (с учётом темы диплома — сниженный микроменеджмент):**
1. Классические «стрелковые» абилки (граната по точке прицела, мина, турель) — повышают требования к прицеливанию и APM.
2. **Ауры/импульсы вокруг героя**: AoE-урон, массовое лечение, самобафф — эффект применяется одной кнопкой без прицеливания.
3. Командные абилки RTS-уровня (глобальный приказ, сканер) — дублируют существующие армейские приказы.

**Выбор:** вариант 2 — три способности «вокруг героя»:
- **Слот 2 «Ударная волна»** (`Shockwave`): 40 урона всем врагам в радиусе 6 м, кулдаун 10 с. Герой влияет на массовый бой одной кнопкой.
- **Слот 3 «Ремонтное поле»** (`RepairField`): лечение 30 HP союзным юнитам в радиусе 8 м, кулдаун 12 с. **Ключевое анти-микро-решение:** вместо ручного отвода раненых юнитов (микро в SC2) игрок лечит армию на месте — позиция героя становится тактическим решением вместо APM.
- **Слот 4 «Перегрузка»** (`Overcharge`): ×2 скорострельность и ×1.5 урон героя на 5 с, кулдаун 15 с. Усиливает TPS-петлю без новых входов.

**Причина:** все три применяются одной кнопкой без прицеливания → нулевой прирост APM при росте тактической глубины (когда войти в толпу, когда лечить, когда бафф). Баланс — в `ManagersTab.EnsureAbilityAsset` (Forge — источник истины), данные — `AbilityData` (data-driven, без хардкода). Эффекты переиспользуют существующие системы: `UnitRegistry` (выбор целей), `Health.TakeDamage`/новый `Health.Heal` (клامп — `CombatLogic.ClampHeal`), `HeroShooter.ApplyOvercharge`. VFX — переиспользование пулов `VfxManager` по событию `AbilityCast` (без новых ассетов — нулевой бюджет).

**Верификация:** EditMode — `AbilityEffectLogicTests` (радиус, клампы, кулдаун-математика); PlayMode — `AbilityTests` (урон/лечение/бафф по фракциям и радиусам).

---

## ADR-014 — Измерительный харнесс фазы улучшения: PerfProbe + Balance-плейтесты + вкладка Improve
**Дата:** 2026-06-11 (Сессия 02, фаза улучшения) · **Статус:** принято

**Проблема:** промпт v3 (§3, §8.2) требует baseline-метрики (FPS/frame time, GC, винрейты AI vs AI) и харнессы до первого улучшения. В v1.0.0 измерительной инфраструктуры нет (Р-2/Р-3 гэп-анализа).

**Варианты:**
1. Unity Profiler вручную в редакторе — не воспроизводимо, не batch, противоречит правилу Forge.
2. Сторонние перф-пакеты (Unity Performance Testing Extension) — тянет зависимость, формат заточен под CI-датабазы Unity.
3. **Собственный лёгкий харнесс**: `PerfStatsLogic` (чистая статика — avg/p95/worst/FPS) + `PerfProbe` (MonoBehaviour, кольцевой буфер `unscaledDeltaTime`, GC-дельта) + PlayMode-тесты категории `Balance` (армия против армии под боевой FSM) с экспортом JSON в `Docs-Vault/Stats/` + вкладка **Improve** в Forge (`ForgeBatch.ExportImprovementMetrics`) — накопительная таблица `Improvements/Metrics.md`.

**Выбор:** вариант 3. **Причина:** нулевые зависимости, полная воспроизводимость одной CLI-строкой (`-runTests -testCategory Balance`), чистая логика тестируется в EditMode, история метрик накапливается в Obsidian (требование v3 §6 «метрики не переписываются»). Сценарии: зеркальный бой 8v8 (проверка симметрии), асимметричный 8v8 Marine vs Grunt (фактический баланс), стресс 20v20 с перф-записью (baseline под нагрузкой).

---

## ADR-015 — Система тултипов HUD: data-driven, единый singleton-слой
**Дата:** 2026-06-11 (Сессия 03, круг 2) · **Статус:** принято

**Проблема:** DoD промпта v2 (§ онбординг/UX) требует тултипы; в v1.1.0 их нет — последний разрыв DoD (Improvements/01, Р-4). Эталон SC2: задержка ~0.4 с, мгновенное переключение между соседними кнопками, контент из данных (название/стоимость/время/описание), позиция у курсора с клампом.

**Варианты:**
1. Тултип-компонент на каждом элементе со своим Canvas — дублирование, расползание стилей.
2. Сторонний ассет тултипов — лишняя зависимость, у популярных нет подтверждённых FOSS-лицензий.
3. **Единый `TooltipSystem`** (singleton на отдельном TooltipCanvas, sortingOrder 100) + `TooltipTrigger` (IPointer-хэндлеры) + `ITooltipProvider` на источниках данных.

**Выбор:** вариант 3. State machine Idle→Pending(0.35 с)→Visible, fade-in 0.12 с (корутина — PrimeTween в проекте отсутствует), SwitchTo без задержки при переходе между кнопками — поведение SC2. Контент тянется из SO (`AbilityData`/`UnitData`/`BuildingData` + новые поля `_description`), производство — динамически из `SelectionSystem.SelectedBuilding`. Форматирование и кламп — чистая статика `TooltipLogic` (EditMode-тесты, инвариантная культура). Сборка в сцену и заполнение описаний — кнопки Forge («Build Tooltip System», «Update Tooltip Descriptions») + `ForgeBatch.SetupTooltips`.

**Грабли (исправлены до мёржа):** второй Graphic на TMP-объекте запрещён (NRE в batch); `Awake()` не вызывается у сохранённого неактивным TooltipView (ленивый геттер CanvasGroup); позиционирование обязано учитывать `canvas.scaleFactor` при CanvasScaler 1920×1080.

---

## ADR-016 — Симметрия зеркальных боёв и завершаемость ретрита
**Дата:** 2026-06-11 (Сессия 03, круг 2) · **Статус:** принято

**Проблема (находки Measure круга 1):** (1) зеркальный бой 8v8 даёт 6:3 — скрытая асимметрия; (2) бои с отступлением не завершаются за 240 сим-с.

**Причина 1 — first-mover advantage:** `_lastAttackTime = 0` разрешал выстрел всем юнитам в первый же кадр, а `Update()` идёт в порядке создания — созданные первыми юниты игрока стреляли раньше ответа противника. **Фикс:** рандомизация фазы первого выстрела `_lastAttackTime = Time.time − cooldown + offset`, offset ∈ [0, cooldown) из hash-mix `EntityId` (`CombatLogic.RandomiseInitialCooldownOffset` — детерминированно, без аллокаций и `UnityEngine.Random`).

**Причина 2 — «слепой» ретрит:** отступивший юнит вставал в Idle на базе со сканом `AggroRadius = 12`, противник в ~42 ед. — юнит больше не видел врагов, обе стороны копили таких на базах до таймаута. **Фикс:** после ретрита в Idle скан расширяется до `AggroRadius × 5`; дополнительно введён data-driven `UnitData.RetreatCooldown` (0 = одноразовый ретрит, прежнее поведение; `CombatLogic.CanRetreatAgain`).

**Альтернативы:** жёсткая синхронизация Update-порядка (хрупко, не решает спавн-порядок); запрет ретрита в Balance-тестах (прячет проблему вместо решения); реген на базе с добиванием преследованием (усложняет FSM без выгоды для анти-микро).

**Верификация:** 8 EditMode-тестов на новые методы `CombatLogic`; Balance-прогон — ожидание: зеркальный бой ≈50/50 на серии, бои завершаются до таймаута.

---

## ADR-017 — Юнит Tank: AoE-сплеш вокруг точки попадания, производство через WarFactory
**Дата:** 2026-06-11 (Сессия 03, круг 3) · **Статус:** принято

**Проблема:** армия из одного типа юнита (Marine) не даёт тактической глубины камень-ножницы-бумага (эталон SC2); бэклог P2 — юнит Tank с AoE и приоритетом зданий.

**Варианты добавления:** (1) правка ядра под новый класс юнита; (2) **data-driven через существующие SO** — два новых поля `UnitData` (`AoeRadius`, `TargetPriority`) + минимальные ветки в `UnitCombat`; (3) компонент-наследник UnitCombat. Выбран (2): новые юниты добавляются ассетами, ядро не растёт.

**Ключевые решения:**
- **Сплеш центрируется на точке попадания (цель), не на танке** — как в SC2. Иначе при AttackRange (5) > AoeRadius (3) выстрел с дистанции никогда не накрыл бы неподвижное здание (баг пойман на Balance-тесте `TankMirror_Resolves` — танки стояли в стейлмейте).
- `CombatLogic.FindTargetsInRadius` — чистая статика без аллокаций (буфер снаружи), EditMode-тесты.
- `TargetPriority.Buildings` — порядок кандидатов в `ScanForTarget`, без новой логики поиска.
- Производство: отдельное здание **WarFactory** (BuildingType.WarFactory), т.к. `ProductionBuilding` несёт один `_unitPrefab`; рефактор под мульти-производство отложен (бэклог).
- ИИ-симметрия: `EnemyCommander._enemyWarFactory` + зеркальные EnemyTank-данные.
- Баланс: HP 280, Damage 25, AoE 3.0, Range 5, CD 2.0, Speed 3.0, Supply 3, Cost 150, ретрит отключён («якорь» позиции). DPS 12.5 vs 10 у Marine, но 3× стоимость и медленность — контрится фокусом одиночек.
- Визуал: craft_speederA.fbx (Kenney Space Kit, CC0), нормализация масштаба по баундам (ADR-012).

**Верификация:** EditMode 217; Balance: `TankVsMarine` (4v8) и `TankMirror` (2v2) сходятся без таймаута.

---

## ADR-018 — Избегание толпы: RVO-тюнинг NavMeshAgent + расширенный формационный spacing
**Дата:** 2026-06-11 (Сессия 03, круг 3) · **Статус:** принято

**Проблема:** юниты слипаются при групповом движении и в бою (pathing-эталон SC2 — без слипания/застреваний).

**Сравнение FOSS-подходов:** (а) тюнинг встроенного RVO NavMeshAgent (HighQualityObstacleAvoidance, радиусы, приоритеты, stoppingDistance); (б) расширение формационных смещений при групповом приказе; (в) separation steering поверх агентов (boids). Выбраны **(а)+(б)**: 80% эффекта (в) при нулевом риске и нулевом новом коде агентов. (в) отклонён: O(N²) без пространственного хэша + прямая запись `agent.velocity` недокументирована в AI Navigation 2.0.

**Параметры:** пехота — radius 0.45, HighQuality avoidance, priority 50, stoppingDistance 0.5; Tank — radius 0.7, priority 30 (его обходят), stoppingDistance 1.0. Формация: дефолтный spacing 1.5 → 2.0; при группе >6 — 2.5 (одна строка в CommandInput).

**Верификация:** PlayMode `CrowdAvoidanceTests` — после группового приказа к одной точке ни одна пара ближе 0.8; разброс атакующих вокруг танка > 1.5.

---

## ADR-019 — Устранение GC-аллокаций боя: таймерные массивы вместо корутин VFX, кэши GetComponent
**Дата:** 2026-06-11 (Сессия 03, круг 4) · **Статус:** принято

**Проблема:** стресс 20v20 показывал managedDelta до ~3.2 МБ за ~3300 кадров боя и worst frame 15.6 мс. Перф-ревью нашло главные источники: (1) `StartCoroutine(DeactivateAfterPlay)` в VfxManager на КАЖДУЮ атаку (до 40/кадр — аллокация state machine корутины); (2) string-конкатенации в HUD (SelectionPanel, ResourceDisplay/HudLogic); (3) `GetComponent` в боевом кадре (range-buffer цели в UnitCombat, циклы EnemyCommander, AudioManager.IsInCombat); (4) `GetActiveScene().name` каждые 0.5 с; (5) `GameObject.Find` rally-маркеров на каждом спавне.

**Решения:**
- VfxManager: массивы `float[]`-таймеров по пулам, декремент в `Update`, ноль аллокаций на выстрел. Публичный API не изменён (тесты совместимы).
- UnitCombat: `SetCurrentTarget()` кэширует `_currentTargetIsBuilding` при захвате цели; `_isHero` в Awake; статический кэш rally-позиций с инвалидацией по `sceneLoaded`.
- `Unit.CachedCombat` (ленивый кэш по образцу CachedHealth) — для EnemyCommander и AudioManager.
- UI: TMP `SetText("{0}", n)`-перегрузы и кэшированный StringBuilder вместо конкатенаций; тултип-провайдеры кэшируют TooltipData по ссылке на SO.
- AudioManager: `_isMainMenuScene` вычисляется один раз.

**Альтернативы:** Unity Pooling API для корутин — не убирает аллокацию енумератора; UniTask — внешняя зависимость (нулевой бюджет — допустимо, но лишнее); инкрементальный GC — прячет симптом, не причину.

**Верификация:** before/after managedDelta в стрессе 20v20 (Improvements/04), полные сьюты зелёные, публичные контракты (HudLogic.FormatCrystals, VfxManager.Play) сохранены.

---

## ADR-020 — Экран статистики матча: чистая модель MatchStats + событийный коллектор
**Дата:** 2026-06-11 (Сессия 03, круг 4) · **Статус:** принято

**Проблема:** после победы/поражения игрок не получает обратной связи по матчу (SC2 — post-game summary); P2 бэклога.

**Решение:** три слоя — `MatchStats` (чистый C#, массивы[2] по Faction: убийства/потери, урон нанесён/получен, кристаллы, производство, пик армии, длительность; 11 EditMode-тестов), `MatchStatsCollector` (MonoBehaviour на GameManagers, подписки на события, ArmyPeak раз в 0.5 с через переиспользуемый буфер), `MatchStatsView` (uGUI-таблица «Вы/Противник» в GameOver-каназе, SetText-перегрузы без аллокаций). Новые события: `Health.AnyDamaged` (static), `ResourceBank.CrystalsMined`. Интеграция: GameWatcher передаёт stats в новые перегрузки `GameOverController.ShowVictory/ShowDefeat(MatchStats)`; старые перегрузки сохранены.

**Решения по краям:** смерть героя не считается потерей юнита (герой респаунится); здания не учитываются в Kill/Lost (в уроне — учитываются); атакующая фракция = противоположная жертве (friendly fire отсутствует).

**Forge:** кнопка «Build Match Stats Panel» (UITab) + `ForgeBatch.SetupMatchStats`. StatsPanel один на GameOver-канвас (поверх обеих панелей) — единственное отступление от спеки, упрощает связку с одним полем `_statsView`.

---

## ADR-021 — Остаточная асимметрия ИИ: блочность EntityId + слабый хэш; faction salt + MurmurHash3
**Дата:** 2026-06-11 (Сессия 03, круг 5) · **Статус:** принято

**Проблема:** после рандомизации фаз (ADR-016) винрейт зеркальных серий накопился 4:14 в пользу Enemy (p≈1.5% при честной монете), при этом победитель не коррелировал с порядком создания команд.

**Найденный механизм (инструментированный Measure, BalanceDiagnosticsTests):** Unity выделяет EntityId последовательно — юниты Player занимают блок [A..A+84], Enemy — соседний блок со сдвигом ~96. Слабый однораундовый multiply-xorshift хэш на двух арифметических прогрессиях даёт систематически смещённые подмножества выходов: одна команда получает в среднем большие cooldown-offset'ы (= раньше стреляет, т.к. `_lastAttackTime = now − cooldown + offset`). Данные: первый выстрел определяет победителя в ~92% раундов (снежный эффект: отступающий не отстреливается); Wang-хэш без соли лишь перевернул направление смещения (Enemy первый выстрел 8/12, винрейт при первом выстреле 100%).

**Фикс:** (1) faction salt — seed XOR-ится с константой фракции до хэширования (блоки ID разносятся по разным областям input-пространства); (2) MurmurHash3 finalizer вместо слабого хэша (полноценный avalanche); (3) InvalidateRallyCache в SetUp Balance-тестов (изоляция раундов).

**After:** 12-раундовая серия 7:5, корреляция победителя с фракцией не обнаружена. Диагностический тест (категория BalanceDiag: лог первого выстрела/убийства/ретрита и корреляций, JSON в Stats/balance-diag.json) оставлен как постоянный инструмент.

**Зафиксированное свойство механики (не баг):** first-shot → победа в ~92% — это снежный эффект дизайна «ретрит без отстрела». При честном 50/50 распределении первых выстрелов серия сходится к 50/50. Изменение самой механики (отстрел при отступлении) — отдельный кандидат бэклога.

---

## ADR-022 — Анимированные модели юнитов: Quaternius Mech Pack + программный AnimatorController
**Дата:** 2026-06-11 (Сессия 03, круг 5) · **Статус:** принято

**Проблема:** юниты — статичные модели без анимаций (сфера 2.11 промпта v3); P2 бэклога. Ограничение: нулевой бюджет, Quaternius на сайте раздаёт через Google Drive (недоступно).

**Ресёрч источников (FOSS/CC0, прямая загрузка):** (1) Quaternius через itch.io/зеркала — CC0, есть боевые анимации; (2) KayKit — CC0, 133 анимации, но ближний бой и фэнтези-стиль; (3) Kenney Animated Characters — CC0, но только idle/jump/run. Выбран **Animated Mech Pack (Quaternius, CC0)** через зеркало OpenGameArt с прямой загрузкой — мехи идеально совпадают с сай-фай стилем (Kenney Space Kit), лицензия подтверждена License.txt в архиве, в FBX встроены клипы Idle/Walk/Run/Shoot/Death (36–40 на модель).

**Реализация:** Marine → Mike.fbx, EnemyGrunt → George.fbx (Stan — резерв); `UnitAnimator` (мост FSM→Animator: IsMoving по скорости агента без аллокаций, триггеры Attack/Die от instance-событий UnitCombat.Attacked и Health.Died); Forge-кнопка «Setup Animated Units (v5)» — импорт-настройки, программная генерация AnimatorController (Idle/Run/Attack/Death) с поиском клипов по ключевым словам (приоритет Shoot над Punch — юниты стреляют), замена Visual в префабах с нормализацией масштаба (ADR-012) и team-color материалами; батч `ForgeBatch.SetupAnimatedUnits`.

**Верификация:** EditMode 232 (+4 UnitAnimatorLogicTests), PlayMode 99 — зелёные; лицензия в `Licenses & Attribution.md`.

---

## ADR-023 — Мульти-производство и command card: ProductionEntry[] с legacy-fallback
**Дата:** 2026-06-11 (Сессия 03, круг 6) · **Статус:** принято

**Проблема:** `ProductionBuilding` нёс один `_unitPrefab` — новый тип юнита требовал нового здания (так появилась WarFactory, ADR-017); P3-долг «мульти-производство». UX-эталон SC2 — command card с кнопками юнитов, хоткеями и очередью.

**Решение:** `BuildingData.ProductionEntries[]` (юнит, цена, время, иконка, хоткей) с правилом «пустой массив = legacy-режим» — ни один существующий ассет/тест не ломается; очередь `Queue<ProductionEntry>` со снапшотом для UI без аллокаций; маппинг префабов `UnitPrefabEntry[]`; хоткеи T/Y/U таблицей в CommandInput; событие `UnitProduced(Unit, ProductionEntry)` (оба подписчика обновлены). Command card в SelectionPanel: 3 кнопки CommandCardButton (ITooltipProvider — тултипы через существующую систему) + 5 слотов очереди QueueSlotUI с прогрессом первого слота — как в SC2. ИИ выбирает тип юнита эвристикой `EnemyWaveLogic.PickProductionEntryIndex` (пехота:танки 3:1, чистая статика — EditMode-тесты).

**Доказательство data-driven тезиса:** новый юнит **HeavyMarine** (HP 150, dmg 14, supply 2, cost 90) добавлен «данными»: ассет + entry в Barracks + префаб из резервного Stan.fbx — ядро не тронуто.

**Грабли:** `GetComponent<T>() ?? AddComponent<T>()` не работает с Unity-объектами (fake-null не триггерит `??`) — MissingComponentException в batch; заменено явной проверкой.

---

## ADR-024 — Fighting retreat: отстрел при отступлении (data-driven)
**Дата:** 2026-06-11 (Сессия 03, круг 6) · **Статус:** принято

**Проблема:** отступающий юнит не отстреливается → первый выстрел решает ~92% зеркальных раундов (снежный эффект, зафиксирован в ADR-021).

**Решение:** `UnitData.FireWhileRetreating` (дефолт true — пехота получает поведение без миграции ассетов): в состоянии Retreating юнит сканирует цели ТОЛЬКО в радиусе атаки (по существующему скан-ритму, без преследования и без остановки движения к rally) и стреляет. Альтернативы: глобальный флаг (не data-driven), полный разворот к цели (ломает смысл ретрита).

**Верификация:** Balance-серия — ожидание снижения first-shot→win корреляции и сохранения завершаемости боёв; цифры в Improvements/06.

---

## ADR-025 — Quick wins: scale-in зданий и стингер волны без новых ассетов
**Дата:** 2026-06-11 (Сессия 03, круг 6) · **Статус:** принято

`BuildingSpawnEffect` — корутина scale-in 0→1.15→1.0 за 0.35 с (overshoot, 12 принципов анимации) на префабах зданий; без аллокаций в кадре. Стингер начала волны ИИ — статическое событие `EnemyCommander.WaveLaunched` + `AudioManager.PlayOneShot(fight.ogg)` через voice-источник: переиспользован существующий CC0-клип, новых ассетов ноль (нулевой бюджет).

---

## ADR-026 — Дерево технологий: TechData/TechRegistry поверх общей очереди производства
**Дата:** 2026-06-12 (Сессия 04, круг 7) · **Статус:** принято

**Проблема:** макро-петля (сфера 2.3) ограничивалась производством юнитов — нет решения «копить на апгрейд или строить армию»; P3-долг «дерево технологий».

**Решение:**
- **Данные:** `TechData` (SO: эффект-enum + величина, цена/время, prerequisites[], InfantryOnly) — три технологии: Бронирование (+20% HP пехоте, 150/40с), Усиленные стволы (+15% урона всем, 175/45с), Расширенные обоймы (−15% кулдаун, 200/50с, требует Стволы). Один ассет на обе фракции.
- **Реестр:** `TechRegistry` (чистый класс, per-faction HashSet, событие TechResearched, кэш множителей per-faction×{пехота,танк} — чтение в боевом кадре без аллокаций; сброс синглтона по SubsystemRegistration).
- **Очередь:** исследование занимает слот ОБЩЕЙ очереди производства (как в SC2) — `ProductionEntry.techData` (null = юнит, полная обратная совместимость); по завершении MarkResearched вместо спавна.
- **Применение статов:** урон/кулдаун — лениво при чтении (эффективные статы в TryAttack), MaxHp — только новым юнитам при ApplyData (как в SC2 — апгрейд не лечит раненых; решение по риску Health.Init).
- **UI:** второй ряд command card (3 кнопки): иконка/цена/хоткей R-G-X, тултип с требованиями, серая при невыполненных prerequisites, зелёная галка «исследовано»; рефреш по событию.
- **ИИ:** `DecideResearch` — заказывает следующую доступную технологию при балансе ≥ цена+50 (резерв на юнита), одно исследование за цикл решений.

**Альтернативы:** отдельная очередь исследований (двойной UI, не SC2-паттерн); модификация полей UnitData (SO шарится между юнитами — нельзя); отдельный union-тип очереди (ломает Queue/снапшот/UI).

**Верификация:** EditMode TechRegistryTests (9), TechCardUITests, ShouldResearch; PlayMode Balance: армия с Усиленными стволами против равной без — победа апгрейженной; Бронирование не трогает танк.

---

## ADR-027 — WebGL-сборка и веб-публикация через GitHub Pages
**Дата:** 2026-06-12 (Сессия 04, круг 8) · **Статус:** принято

**Проблема:** демонстрация диплома требует установки Windows-билда; стретч-цель промпта v2 §2.18 — веб-страница с игрой (бесплатно).

**Решения:**
- **Сборка:** кнопка Forge «Build WebGL (v8)» + `ForgeBatch.BuildWebGL` (паттерн BuildWindows). Gzip + **decompressionFallback** — работает на статическом хостинге без Content-Encoding-заголовков (GitHub Pages). WebGL2, шаблон Default.
- **Платформенный guard:** единственный runtime-код с файловой системой — ScreenshotDirector (авто-скриншоты M11); обёрнут `#if !UNITY_WEBGL || UNITY_EDITOR`.
- **Хостинг — GitHub Pages, не itch.io:** itch.io требует личный аккаунт владельца (создание аккаунта за пользователя — за пределами авторизованной автономии); GitHub уже авторизован (репозиторий, релизы). Ветка gh-pages с содержимым Builds/WebGL. itch.io остаётся пользователю — загрузить тот же zip.

**Риски/ограничения:** размер .data-файла (LFS-модели) — лимит Pages 100 МБ/файл; URP на WebGL2 — без специфичной оптимизации (отдельный круг при необходимости); перф в браузере ниже нативного.

---

## ADR-028 — Лэйаут карты: центральный чокпоинт, экспанды, точечная симметрия
**Дата:** 2026-06-12 (Сессия 04, круг 9) · **Статус:** принято

**Проблема:** карта — пустой квадрат 100×100 с базами по углам: нет тактической географии (сфера 2.15; эталон SC2 — чокпоинты, экспанды, симметрия).

**Решения:**
- **Чокпоинт:** 6 скал (rock_largeA, CC0) двумя колоннами x=±8 (сужение прохода до ~11 ед.) + открытые фланги — три пути между базами с разным риском. Непроходимость — **NavigationStatic-запекание**, не NavMeshObstacle (carve нестабилен для статики в AI Navigation 2.0, бейк детерминирован для тестов).
- **Экспанды:** по ResourceNode ×2000 (двойной запас) на сторону, ближе к центру — риск/награда; игрок занимает Extractor'ом, ИИ-экспанд вне скоупа.
- **Симметрия:** точечная 180° (SC2-паттерн для 1v1); базы отодвинуты на (±35,±35), здания/герой/камера перенесены явным обновлением позиций (Setup-методы только создают, не переставляют).
- **Сет-дрессинг:** 15 CC0-объектов без коллайдеров и NavigationStatic.
- Forge: «Rebuild Map Layout (v9)» + ForgeBatch.RebuildMapLayout (включая перебейк NavMesh и сохранение).

**Верификация:** PlayMode MapLayoutTests — путь между базами существует (PathComplete), экспанды на NavMesh и добываемы, центр скалы вне NavMesh; полные сьюты без регрессий.

**Бэклог-наблюдение:** RtsCameraController не ограничивает панораму — с увеличенной картой стоит добавить мягкий кламп.

---

## ADR-029 — Локализация ru/en: LocService + SO-таблица, без пакета Unity Localization
**Дата:** 2026-06-12 (Сессия 04, круг 10) · **Статус:** принято

**Проблема:** весь UI на русском — демонстрация/публикация для англоязычной аудитории невозможна (P3 «локализация EN»); защита диплома — русская, значит ru остаётся дефолтом.

**Сравнение подходов:** (1) пакет Unity Localization — оверкилл для 2 языков (Addressables-асинхронность для WebGL, высокая кривая входа); (2) CSV в StreamingAssets — в WebGL только через UnityWebRequest (async ломает простую инициализацию); (3) **SO-таблица LocTable + статический LocService** — синхронно, ноль зависимостей, нулевые аллокации чтения (Dictionary-кэш), вписывается в SO-паттерн проекта. Выбран (3).

**Решения:**
- `LocService` (CurrentLanguage → PlayerPrefs `Settings.Language`, событие LanguageChanged, Get(key) с fallback на сам ключ — отсутствующий перевод виден сразу); `LocServiceBootstrap` в обеих сценах; `LocalizedText` для статических надписей (автоподписка).
- SO-данные: параллельные поля `_displayNameEn/_descriptionEn` (fallback на ru при пустых) — а не вынос в таблицу: Forge уже programmatically заполняет тексты, миграция одной кнопкой.
- Динамические строки — формат-ключи (`{0}` обрабатывает TMP SetText без аллокаций); кэши тултипов инвалидируются по LanguageChanged.
- Переключатель RU/EN в настройках (опции не локализуются — самореференция).
- Forge: «Migrate Localization EN Fields», «Create/Update LocTable» (56 ключей), «Apply Localization to Scene UI» + батч SetupLocalization (Sandbox + MainMenu).

**Попутно (quick win):** мягкий кламп RTS-камеры к границам карты ±48 (чистая ClampPosition + EditMode-тесты) — долг из ADR-028.

**Верификация:** LocServiceTests (язык/событие/fallback/полнота таблицы), обновлённые TooltipLogic-тесты с LocService-фикстурой, RtsCameraClampTests; полные сьюты.

---

## ADR-030 — Интеграционный плейтест реальной сцены; найден и устранён критический NavMesh-баг
**Дата:** 2026-06-12 (Сессия 04, круг 11) · **Статус:** принято

**Проблема:** все PlayMode-тесты создавали синтетическую геометрию — реальная сцена Sandbox не была покрыта ни одним автотестом (сфера 2.17 «протокол плейтеста»). Цена пробела проявилась немедленно.

**Решение — SceneIntegrationTests** (категория SceneIntegration, загрузка реальной сцены):
- SmokeTest: все менеджеры/HUD/слои сцены/MapLayout/NavMesh у баз.
- FullMatch_AiVsAi_Terminates: полный матч на реальной карте завершается; метрики (прохождение волн через чокпоинт, пик юнитов, длительность) в Stats/scene-integration.json.
- PerfDuringWave: PerfProbe в разгаре волны (avg 3.4 мс — бюджет соблюдён).

**Найденные продакшен-баги (ушли в v1.9.0/v1.10.0 — синтетика не ловила):**
1. **NavMeshObstacle extents × scale** — carve-экстенты зданий задавались в локальных координатах и умножались на scale (WarFactory: 2.5 × 5 = 12.5 м → дыра 25×25 м): зона баз выпадала из NavMesh, агенты у базы не создавались, юниты не доставали до HQ — **матч нельзя было ни выиграть, ни проиграть**. Фикс: extents 0.5³ (carve 1.5–2.5 м по факту размера), SamplePosition-snap при спавне из зданий.
2. **Контратака отступивших через карту**: пост-ретритный скан ×10 (ставился ради сходимости синтетических боёв, ADR-016/круг 6) на реальной карте отправлял отступивших юнитов в одиночку сносить вражеский HQ (нарушение приказов/анти-микро). Фикс на правильном слое: геймплейный скан → оборонительный ×2; сходимость синтетики — **stall-breaker в тест-харнессе** (при неизменном суммарном HP 30 сим-с обеим сторонам выдаётся AttackMove — модель приказа командира, которого в синтетике нет).

**Третий (самый глубокий) баг — враг производил юнитов фракции ИГРОКА.** Телеметрия FullMatch (балансы/состояния/смерти) показала: враг тратит кристаллы, но вражеская армия = 0, а армия игрока растёт сама. EditMode-инспекция сцены (новый SceneWiringTests, мгновенная против минут PlayMode) дала точную картину: v6-маппинг `_unitPrefabs` прошивался в ОБЩИЙ Barracks.prefab (Marine→TestUnit/Player), и вражеские инстансы его унаследовали; legacy-`_unitPrefab` инстанса (EnemyUnit) затенялся приоритетом маппинга. Баг жил с круга 6 и маскировался «мстителями» ×10-скана («Player wins» в первой версии интеграционного теста был ложно-зелёным: вражеский барак кормил армию игрока). **Фикс:** собственные BuildingData врага (EnemyBarracks/EnemyWarFactory.asset с entries EnemyGrunt/EnemyTank) + пер-инстансный маппинг префабов (Forge `FixEnemyProductionV11`); попутно «голодание волн» (DecideWave требовал строго Idle — в толпе у rally юниты вечно Moving) → призыв всех вне боя; чокпоинт расширен ±8→±12.

**After:** полный матч на реальной сцене: Enemy wins за ~150 сим-с, 11 проходов чокпоинта, армия врага растёт, бои идут. SceneWiringTests (2) — постоянная EditMode-защита: «каждое производственное здание разрешает спавнящийся префаб СВОЕЙ фракции».

**Уроки (для диплома):** (а) симуляционные тесты не заменяют интеграционные на реальном контенте — три продакшен-бага жили под 280+ зелёными синтетическими тестами; (б) NavMeshObstacle.size — в локальном пространстве, масштаб умножает carve; (в) маппинги в общих префабах наследуются всеми инстансами — фракционные переопределения делать пер-инстанс; (г) ложно-зелёный тест опаснее красного: «Player wins» скрывал сломанное производство врага; (д) телеметрия в тесте + мгновенная EditMode-инспекция сцены дешевле гипотез; (е) Unity Test Framework сбрасывает Time.timeScale между yield'ами; (ж) -quit несовместим с -runTests; (з) духовное «Time.time — масштабированное время» — не путать с реальным в логах.

---

## ADR-031 — Game feel героя: централизованный GameFeelManager, Diablo — вторичный референс
**Дата:** 2026-06-12 (Сессия 04, круг 12) · **Статус:** принято

**Контекст:** пользователь предложил сменить референс на серию Diablo (Idea.txt). Решение (согласовано): основной референс остаётся SC2 (тема диплома — RTS/TPS камера и управление, Diablo не покрывает ни то, ни другое); **Diablo принят вторичным референсом для героя и game feel** — вес/импакт ударов, сочность откликов (сфера 2.12).

**Архитектура:** централизованный `GameFeelManager` (на GameManagers) против размазывания эффектов по системам — подписки на существующие шины (HeroShooter.ShotFired, Health.AnyDamaged, AbilitySystem.AbilityCast), все константы в `GameFeelSettings.asset` (SO — отдельный цикл тюнинга feel без перекомпиляции).

**Эффекты:** отдача камеры на выстрел (ручной экспоненциальный спад — без PrimeTween/Impulse-каналов) + вариация питча звука (×1.5 в Overcharge); хит-флэш целей через MaterialPropertyBlock (без инстансов материалов, HasProperty-guard, работает на SkinnedMeshRenderer мехов); нокбэк пехоты NavMeshAgent.Warp с SamplePosition-страховкой (танки исключены по радиусу агента); Shockwave — screen shake + хит-стоп (timeScale-дип 0.06с реального времени) + процедурное кольцо частиц; RepairField — зелёный пульс союзников в радиусе; Dash — TrailRenderer-смазка; дульная вспышка на каждый выстрел; смерти сочнее (startSize 1.8).

**Guards хит-стопа:** не запускается при паузе (timeScale<0.1) и в batch-режиме (не ломает SceneIntegration); прерывается событием PauseController.PauseChanged (добавлено) с безопасным сбросом.

**Forge:** вкладка GameFeel (создание SO, прошивка менеджера/префабов/трейла/кольца) + батч SetupGameFeel.
