# Сессия 01 — 2026-06-10

**Фаза:** 0 — Предварительный ресёрч и инфраструктура
**Активный майлстоун:** M0 — Инфраструктура

---

## Фаза 0 — Результаты ресёрча пререквизитов

Проведён предварительный ресёрч по всем блокирующим вопросам перед стартом разработки. Полные итоги — в [[Research/00 - Prerequisites]].

### A. Unity и пакеты

- Выбрана **Unity 6.3 LTS (6000.3.x)** — текущая рекомендация Unity, поддержка до декабря 2027. Unity 6.0 отклонена (конец поддержки октябрь 2026), 6.4 отклонена (нестабильная ветка).
- Подтверждён стек пакетов: Input System 1.19, Cinemachine 3.1, AI Navigation 2.0, Test Framework 1.6, uGUI 2.0 (TMP встроен). Пакет Behavior (behavior trees) исключён — с мая 2026 в maintenance-only.
- Подтверждено: uGUI для всего UI (world-space UI Toolkit слишком нов — лишний риск).
- Твининг: PrimeTween (MIT, zero-alloc) — вместо DOTween (closed-source, NRE-ловушки).

### B. Аудио-пайплайн

Все облачные сервисы (Suno, Udio, ElevenLabs, Stable Audio API, Azure TTS) исключены ограничением нулевого бюджета. Выбраны:
- Музыка: incompetech/Kevin MacLeod (CC-BY 4.0) + Pixabay + OpenGameArt.
- SFX: Kenney Sci-fi Sounds (CC0) + Sonniss GDC 2026 (royalty-free).
- Голоса юнитов: Kenney Voiceover Pack + Voiceover Pack (Fighter) — CC0, ноль усилий.
- Локальная генерация (опционально): Stable Audio Open (открытые веса), Piper TTS (CPU).

### C. Арт / визуал

Платные паки (Synty POLYGON) исключены по нулевому бюджету и неподходящей EULA (нельзя в открытый репо). Выбраны:
- Юниты: Quaternius Animated Mech Pack + Animated Tanks Pack (CC0).
- Здания: Quaternius Ultimate Modular Sci-Fi (CC0).
- Пропсы/оружие: Kenney Blaster Kit + Space Kit (CC0).
- UI: Kenney UI Pack Sci-Fi (CC0) + game-icons.net (CC-BY 3.0, атрибуция в Credits).
- Шрифты: Russo One + Exo 2 (OFL, кириллица есть).

### D. VCS и CI

- LFS настраивается до первого коммита, выборочно: крупные бинарники в LFS, мелкие png/ogg — обычный git (ограничение: 10 GiB/мес трафика на free-tier).
- Релизы — GitHub Releases (2 GiB/файл, трафик бесплатен).
- CI (GameCI v4 + GitHub Actions): Actions для публичных репо бесплатны, но нужен секрет `UNITY_LICENSE`. **Решение: стартуем без CI**, добавим после M0 по желанию пользователя.

---

## Применено ограничение нулевого бюджета (ADR-009)

Зафиксировано как жёсткая директива: $0 навсегда. Пересмотрены ADR-006 (аудио) и ADR-007 (арт). Все последующие решения проходят проверку: «бесплатен навсегда? без карты? не триал?»

---

## Архитектурные решения (ADR-001..009)

Все девять ADR приняты и зафиксированы в [[04 - Decision Log]]:

| ADR | Тема | Выбор |
|---|---|---|
| ADR-001 | Версия Unity | Unity 6.3 LTS |
| ADR-002 | Cinemachine | 3.1 (не 2.x) |
| ADR-003 | UI-система | uGUI + TMP |
| ADR-004 | Твининг | PrimeTween |
| ADR-005 | ИИ юнитов | Своя FSM |
| ADR-006 | Аудио-пайплайн | CC0/CC-BY библиотеки + локальная генерация |
| ADR-007 | Арт | Kenney + Quaternius CC0 |
| ADR-008 | VCS / CI | Выборочный LFS; CI — опционально |
| ADR-009 | Бюджет | $0, нулевой бюджет — приоритет выше всего |

---

## Выполнено в рамках M0

- Переименование проекта в `diplomaGame`.
- Настройка git-инфраструктуры: `.gitignore` (Unity), `.gitattributes` (LFS-паттерны, YAML LF), `git lfs track`.
- CI workflows: `tests.yml` и `build-release.yml` (GameCI v4) — файлы созданы, секреты — задача пользователя.
- Cinemachine 3.1.6 добавлен в `Packages/manifest.json`.
- Скелет `Project Forge` (`Assets/_Project/Editor/ProjectForge/`).
- Создано хранилище `Docs-Vault/` — 7 документов.

---

## Что дальше (оставшиеся задачи M0)

- [x] **Первый коммит** — `bdcb962`, 117 файлов, 9857 строк.
- [x] **Создать репозиторий на GitHub** — https://github.com/arcsymer/diplomaGame (публичный).
- [x] **`git push origin main`** — выполнен (пользователь авторизовал gh в этой сессии).
- [ ] **Секреты CI** (решение пользователя: CI делаем) — пользователь активирует Personal-лицензию в Hub (появится `C:\ProgramData\Unity\Unity_lic.ulf`) и добавляет секреты `UNITY_LICENSE`, `UNITY_EMAIL`, `UNITY_PASSWORD`. До этого workflow Tests будет красным — это ожидаемо.
- [ ] Тег `v0.1.0` на M0 — после зелёного CI.
- [ ] После M0 — старт M1: Cinemachine 3.1, стейт-машина GameModeController, 2 Action Map.

---

## M1 — Режимы и камеры (выполнен в этой же сессии)

- **Runtime-код** (`Assets/_Project/Scripts/`): `GameMode` (enum), `ModeSwitchLogic` (чистая статика — приоритеты камер, Toggle; тестируется без сцены), `GameModeController` (MonoBehaviour: переключение по Tab, приоритеты Cinemachine-камер 20/10, включение/выключение ActionMap, событие `ModeChanged`, без утечек подписок).
- **Input Actions** (`Assets/_Project/Settings/GameControls.inputactions`): карты RTS (SwitchMode/Select/Command/PanCamera/ZoomCamera) и TPS (SwitchMode/Move/Look/Fire/Aim/Ability1-4) — биндинги готовы, обработчики появятся в M2/M3.
- **Forge**: вкладка Managers — кнопка **Setup Mode Rig (M1)** (идемпотентно: Hero-капсула, RtsCameraTarget, CinemachineBrain, RTS/TPS-камеры, GameManagers с проставленными через SerializedObject ссылками); `ForgeBatch` — batch-фасад для CLI (`-executeMethod`); валидатор проверяет ссылки контроллера.
- **Сцена**: `Sandbox.unity` создана и настроена batch-вызовом ForgeBatch (не руками!), добавлена в Build Settings.
- **Тесты: EditMode 9/9 ✅, PlayMode 8/8 ✅.**

### Пойманные API-грабли Cinemachine 3.x (для пояснительной записки)
1. `CinemachineRotationComposer` не имеет `LookAt` — цель задаётся на самой `CinemachineCamera` (компилятор поймал, исправлено).
2. `CinemachineCamera.Priority` — структура `PrioritySettings`, не int: присваивание int работает (неявная конверсия), но в Assert сравнивать нужно `Priority.Value` (поймано PlayMode-тестом, исправлено).

### CI: починка образа
Docker-образа GameCI для 6000.4.9f1 не существует (есть .8 и .10) — оба workflow запинены на `unityci/editor:ubuntu-6000.4.10f1-*` (ADR-008 UPD). Секреты `UNITY_LICENSE`/`UNITY_EMAIL`/`UNITY_PASSWORD` добавлены пользователем.

---

## M2 — RTS-управление (выполнен в этой же сессии)

- **Юниты** (`Scripts/Units/`): `Unit` (NavMeshAgent-движение, состояния Idle/Moving/Holding/Patrolling), `UnitCommand` (Move/AttackMove/Hold/Patrol; attack-move пока без боя — ведёт себя как move до M4), `UnitCommandLogic` (чистая логика: патруль A↔B, прибытие, формационные смещения — юниты не толкаются в одну точку), `UnitRegistry` (реестр без аллокаций).
- **Выделение** (`Scripts/Selection/`): клик, рамка (с учётом инверсии Y экранных координат GUI), Shift-аддитив, контрол-группы Ctrl+1..5 / 1..5, событие SelectionChanged, кольцо выделения у ног юнита.
- **Приказы** (`Scripts/Commands/CommandInput.cs`): RMB → move (A — attack-move, P — patrol), H — hold.
- **RTS-камера** (`Scripts/CameraControl/`): WASD-панорамирование, зум скроллом (масштаб FollowOffset с сохранением угла), активна только в RTS-режиме.
- **Forge**: вкладки NavMesh (запекание) и Prefabs (TestUnit-префаб), кнопка Setup RTS Control (M2), `ForgeBatch.SetupM2` — сцена настроена batch-вызовом: NavMesh запечён, 5 юнитов у базы.
- **Rally-points перенесены в M5** — без зданий и производства они бессмысленны (решение зафиксировано здесь).
- **Тесты: EditMode 38/38 ✅, PlayMode 12/12 ✅** (вкл. рантайм-запекание NavMesh в PlayMode-тесте).

---

## M3 — TPS-герой (выполнен в этой же сессии)

- **Герой — двойная природа** (ключевое решение): в RTS — обычный юнит (NavMeshAgent + Unit, выделяется и получает приказы), в TPS — прямой контроль (CharacterController). Переключение по `ModeChanged`: в TPS `agent.enabled=false`, CC включён; обратно — наоборот. Подписка в Awake/OnDestroy (не OnEnable/OnDisable — self-disable разорвал бы подписку).
- **Управление** (`Scripts/Hero/`): `HeroController` (WASD относительно yaw героя, поворот мышью, гравитация; чистая `HeroMovementLogic`), `HeroShooter` (рейкаст из центра экрана, кулдаун `FireRateLogic`, интерфейс `IDamageable` — задел M4, событие `ShotFired` — задел VFX M8, простейший прицел в OnGUI до M6), `AbilitySystem` (4 слота на ScriptableObject `AbilityData`, кулдауны `AbilityCooldownLogic`, Dash работает — рывок вперёд, слоты 2–4 заглушки до M4+).
- **Forge**: кнопка Setup Hero (M3) — компоненты героя, SO-ассеты способностей (`Data/Abilities/`), все ссылки через SerializedObject; `ForgeBatch.SetupM3`.
- **Тесты: EditMode 64/64 ✅, PlayMode 24/24 ✅.**

### Пойманные баги (материал для записки)
1. Float-точность на границе кулдауна: `now - last >= cooldown` ложно при точном равенстве из-за погрешности вычитания — введён эпсилон 1e-4.
2. `AbilityCooldownLogic.Tick` возвращал отрицательный остаток — кламп к нулю (поймано EditMode-тестом).
3. PlayMode + физика: свежесозданный коллайдер невидим для Raycast в том же кадре — нужен `Physics.SyncTransforms()`; отложенный `Destroy` в TearDown оставлял куб предыдущего теста на пути луча — в тестах нужен `DestroyImmediate`.

---

## M4 — Бой и ИИ (выполнен в этой же сессии) — ядро темы диплома

- **Статы в данных**: `UnitData` (ScriptableObject) — hp, урон, дальность, кулдаун, aggro-радиус, скорость, порог отступления; ассеты `Marine` и `EnemyGrunt` создаются Forge-кнопкой Config.
- **Здоровье**: `Health` (IDamageable) — события Damaged/Died, смерть однократна, GO уничтожает слушатель.
- **Умный ИИ юнита** (`UnitCombat`, состояния None/Engaging/Attacking/Retreating) — реализация «сниженного микроменеджмента»:
  - **авто-выбор целей** (сканирование раз в 0.25с, ближайший враг, без аллокаций);
  - **авто-отступление** при HP < 25% к своей базе; прямой приказ игрока отменяет отступление (игрок главнее автоматики);
  - **поведение зависит от приказа**: Move — игнорирует врагов, Hold — атакует в радиусе, но не преследует, AttackMove — расширенный поиск по пути, Idle/Patrol — стандартная агрессия.
- **Чистая логика** в `CombatLogic` (ближайшая цель по sqrMagnitude, порог отступления, дальность, точка отхода) — полностью покрыта EditMode-тестами.
- **Forge**: вкладка Config (UnitData-ассеты), EnemyUnit-префаб (красный URP-материал), Setup Combat (M4): Health у героя (150 HP), переспавн юнитов, 3 врага у вражеской базы; `ForgeBatch.SetupM4`.
- **Тесты: EditMode 78/78 ✅, PlayMode 32/32 ✅** (бой до смерти, отступление, Hold-неподвижность, однократность смерти).

### Пойманный дефект дизайна (материал для записки)
Тестовая сцена без маркеров баз дала rally-точку (0,0,0) = позиции юнита — «отступление» завершалось мгновенно, и юнит вставал в оборону (Attacking). Вывод: инварианты сцены (маркеры баз) — часть контракта ИИ; тестовая среда должна их воспроизводить. Тест исправлен; поведение «юнит у базы защищается» признано корректным.

---

## M5 — Экономика и постройки (выполнен в этой же сессии)

- **Один ресурс — «Кристаллы»**, и никаких рабочих: авто-добыча реализована как пассивный доход от HQ и Extractor'ов, построенных у ResourceNode (запас исчерпаем). Это прямое воплощение «сниженного микро»: игрок принимает решения «где и что строить», а не таскает рабочих.
- **Здания** (`Scripts/Buildings/`): `Building` (Health, доход по тикам, реестр `BuildingRegistry`), `ProductionBuilding` (очередь до 5, прогресс, спавн юнита с авто-приказом идти на **rally-point** — перенесённый долг M2 закрыт), `BuildingPlacer` (режим строительства: B — Barracks, E — Extractor, призрак с зелёной/красной валидацией, проверка пересечений и близости ноды, списание стоимости).
- **Экономика** (`Scripts/Economy/`): `ResourceBank` (балансы фракций, события), `ResourceNode`, чистая `EconomyLogic` (включая устойчивый к лагам подсчёт тиков дохода целочисленным делением).
- **Управление**: клик по своему зданию выбирает его; RMB — rally-point, T — заказать юнита.
- **Data**: `BuildingData` SO (HQ 1000hp/доход 5, Barracks 100💎/Marine за 50💎/5с, Extractor 75💎/доход 8); ассеты и префабы (NavMeshObstacle carve) — кнопками Forge; `ForgeBatch.SetupM5` расставил в сцене HQ обеих фракций, Barracks и 4 ноды.
- **Тесты: EditMode 101/101 ✅, PlayMode 49/49 ✅** (банк, ноды, пассивный доход, производство с rally, отказ при нехватке средств).
- Мелкие пойманные ошибки компиляции: неоднозначность `Object`/`UnityEngine.Object`, недостающий using — исправлены до коммита.

---

## M6 — Полный UX/UI (выполнен в этой же сессии, в два этапа)

**M6a — игровые HUD:**
- RTS HUD: панель ресурсов (события BalanceChanged + пульс-juice), панель выделения (юниты или здание с прогрессом производства и очередью), миникарта (орто-камера → RenderTexture), подсказки клавиш.
- TPS HUD: uGUI-прицел (OnGUI-версия удалена), полоса HP героя, 4 слота способностей с кулдаун-заливкой.
- World-space полосы HP над юнитами/зданиями (билборд, скрыты при полном HP — меньше шума).
- Фидбек приказов: пул анимированных маркеров (зелёный move / красный attack-move), MaterialPropertyBlock без аллокаций.

**M6b — меню и системные экраны:**
- Сцена MainMenu (индекс 0 в Build Settings): Play/Settings/Quit.
- Настройки (общая панель в меню и паузе): качество графики, fullscreen, 3 громкости (музыка/SFX применятся в M7 через AudioMixer — TODO зафиксирован), чувствительность мыши; всё в PlayerPrefs через SettingsService + чистая SettingsLogic.
- Пауза (Escape, timeScale=0; уступает Escape'у отмены строительства), экраны Победа/Поражение (API для M9).

**Пойманные грабли (материал для записки):**
1. `AddComponent<TMP_Text>` молча возвращает null — класс абстрактный; нужен `TextMeshProUGUI`.
2. TMP без «Essential Resources» кидает NRE в SetText; `AssetDatabase.ImportPackage` в batch-режиме асинхронен и не успевает — решено распаковкой .unitypackage (это tar.gz) напрямую в Assets с сохранением .meta/GUID.
3. `Toggle.graphic` — публичное поле: сериализуется как `graphic`, а не `m_Graphic` (в отличие от приватных `m_FillRect`/`m_OnClick`).

**Тесты: EditMode 130/130 ✅, PlayMode 60/60 ✅.**

---

## M7 — Аудио (выполнен в этой же сессии)

- **Контент при нулевом бюджете**: скачаны напрямую (без аккаунтов) 4 пака Kenney CC0 (Sci-Fi Sounds, Interface Sounds, Impact Sounds, Voiceover Pack Fighter) + 3 трека Kevin MacLeod CC-BY 4.0 с incompetech ("Floating Cities" — меню, "Deep Haze" — эмбиент, "Volatile Reaction" — бой). В проект отобрано 30 клипов (16 МБ). Всё внесено в Licenses & Attribution с готовой строкой атрибуции для титров.
- **AudioMixer** `GameMixer.mixer` (Master → Music/SFX/UI/Voice) создаётся Forge-кнопкой через reflection над internal `AudioMixerController`; код устойчив к провалу reflection (fallback — категории громкости в AudioManager).
- **AudioManager**: музыкальная стейт-машина (Menu/Ambient/Combat, кроссфейд 1.5с; бой определяется по состояниям юнитов и недавней стрельбе героя), пул SFX-источников, голоса юнитов с rate-limit 1.5с (на выделение и приказы), UI-звуки на всех кнопках (UiButtonSound).
- **Привязка к событиям**: выстрелы героя/юнитов, смерти (взрывы позиционно), постройка/ошибка размещения, производство юнитов, победа/поражение («flawless victory» / «game over» голосом диктора).
- **Настройки**: ползунки Music/SFX/Voice из M6 теперь реально применяются (экспонированные параметры микшера или fallback).
- **Тесты: EditMode 142/142 ✅, PlayMode 73/73 ✅.**
- Пойманное: тест ack-голосов получал false не из-за rate-limit, а из-за пустого массива клипов (null-вход InitForTest) — тест переписан на фейковые AudioClip.Create.

---

## M8 — Визуал (выполнен в этой же сессии)

- **CC0-модели** (скачаны напрямую с kenney.nl, без аккаунтов): Blocky Characters (юниты/герой), Space Kit (ангары-здания, кристаллы для ресурсных нод, камни/кратеры-декор), Blaster Kit (оружие героя), Particle Pack (текстуры VFX). Quaternius отпал — раздаёт только через Google Drive (нельзя забрать скриптом); зафиксировано.
- **Подмена визуала через Forge**: у префабов скрыт примитив-рендер (коллайдеры нетронуты — выделение работает), добавлен child Visual с FBX-моделью; материалы конвертированы в URP (ремап _Color→_BaseColor); фракционные материалы UnitBlue/UnitRed; герой получил бластер в руку; 9 декор-объектов на карте.
- **VFX**: 4 префаба частиц (вспышка выстрела, попадание, взрыв, эффект постройки) построены кодом на текстурах Particle Pack; VfxManager с пулами по 6, подписан на те же события, что и звук (выстрелы/смерти/постройка).
- **Свет и пост-обработка**: тёплое солнце с мягкими тенями, Trilight ambient, тёмный процедурный скайбокс, тёмная земля, URP Volume (Bloom, Vignette, ColorAdjustments, ACES), HDR-камера.
- **Тесты: EditMode 152/152 ✅, PlayMode 81/81 ✅** (пойман off-by-one в тесте циклического пула — реализация была верной, исправлен тест).

---

## M9 — Сценарий (выполнен в этой же сессии)

- **ИИ-противник** (`Scripts/AI/`): EnemyCommander раз в 2с принимает решения — производит юнитов из своего барака (пока есть деньги и < 12 юнитов) и отправляет волны attack-move на HQ игрока (размер волны эскалирует со временем матча: 3 → 5 → 7; запуск по накоплению или таймауту 30с). Чистая EnemyWaveLogic полностью покрыта тестами.
- **Победа/поражение**: GameWatcher следит за Health обоих HQ → ShowVictory/ShowDefeat (+ событие MatchEnded, голос диктора и музыка уже подключены через M7).
- **Респаун героя**: смерть героя — не поражение, а штраф: корутина 8с → телепорт на базу с полным HP (юнит-смерть героя больше не уничтожает его GO).
- **Forge**: Setup Scenario (M9) — вражеский барак (производит EnemyUnit), EnemyCommander + GameWatcher с проставленными ссылками; `ForgeBatch.SetupM9`.
- **Тесты: EditMode 169/169 ✅, PlayMode 84/84 ✅** (победа при смерти вражеского HQ, поражение при смерти своего, авто-производство ИИ).
- Пойманное: TearDown падал MissingReference на объектах, уничтоженных самим тестом (отложенный Destroy зданий) — добавлен null-guard; третий случай ambiguous `Object` подряд — паттерн зафиксирован для ревью.

---

## UPD конца сессии — фактические отклонения

1. **Unity: фактически установлена 6000.4.9f1 (ветка 6.4, Supported)**, а не рекомендованная 6.3 LTS — пользователь уже создал проект на ней. Решение: остаёмся на 6.4 (ADR-001 дополнен). Пакеты сверены: URP 17.4, Input System 1.19, AI Navigation 2.0.12, Test Framework 1.6, Cinemachine 3.1.6 — всё совместимо, EditMode-тесты 3/3 зелёные.
2. **CI: пользователь решил делать** (ADR-008 дополнен) — workflows уже в репо.
3. Рабочее название подтверждено: **diplomaGame** (финальное — позже).
